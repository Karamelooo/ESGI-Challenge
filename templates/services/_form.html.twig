{{ form_start(form) }}
<div class="min-h-screen flex justify-center">
    <div class="p-16 rounded shadow-2xl w-full">
        <h2 class="text-3xl font-bold mb-10 text-gray-800">Gestion service</h2>

        {{ form_start(form) }}

        <div class="grid grid-cols-1 md:grid-cols-1 gap-6">
            <div>
                {{ form_label(form.name, 'Nom', {'label_attr': {'class': 'block mb-1 font-bold text-gray-500'}}) }}
                {{ form_widget(form.name, {'attr': {'class': 'w-full border-2 border-gray-200 p-3 rounded outline-none focus:border-purple-500 dark:border-gray-800 dark:bg-onyx-800'}}) }}
                {{ form_errors(form.name) }}
            </div>

            <div>
                {{ form_label(form.category, 'Famille', {'label_attr': {'class': 'block mb-1 font-bold text-gray-500'}}) }}
                {{ form_widget(form.category, {'attr': {'class': 'w-full border-2 border-gray-200 p-3 rounded outline-none focus:border-purple-500 dark:border-gray-800 dark:bg-onyx-800'}}) }}
                {{ form_errors(form.category) }}
            </div>

            <div>
                {{ form_label(form.purchase_price, 'Prix d\'achat', {'label_attr': {'class': 'block mb-1 font-bold text-gray-500'}}) }}
                {{ form_widget(form.purchase_price, {'attr': {'class': 'w-full border-2 border-gray-200 p-3 rounded outline-none focus:border-purple-500 dark:border-gray-800 dark:bg-onyx-800'}}) }}
                {{ form_errors(form.purchase_price) }}
            </div>

            <div>
                {{ form_label(form.selling_price, 'Prix de vente', {'label_attr': {'class': 'block mb-1 font-bold text-gray-500'}}) }}
                {{ form_widget(form.selling_price, {'attr': {'class': 'w-full border-2 border-gray-200 p-3 rounded outline-none focus:border-purple-500 dark:border-gray-800 dark:bg-onyx-800'}}) }}
                {{ form_errors(form.selling_price) }}
            </div>

            <div>
                {{ form_label(form.tax, 'TVA', {'label_attr': {'class': 'block mb-1 font-bold text-gray-500'}}) }}
                {{ form_widget(form.tax, {'attr': {'class': 'w-full border-2 border-gray-200 p-3 rounded outline-none focus:border-purple-500 dark:border-gray-800 dark:bg-onyx-800'}}) }}
                {{ form_errors(form.tax) }}
            </div>
        </div>

        <div class="mt-6">
            <button class="block w-full bg-green-500 hover:bg-green-300 p-4 rounded text-white hover:text-white transition duration-300">Enregistrer</button>
            <a href="{{ path('app_services_index') }}">Retour à la liste</a>
        </div>

        {{ form_end(form) }}
    </div>
</div>
{{ form_end(form) }}
