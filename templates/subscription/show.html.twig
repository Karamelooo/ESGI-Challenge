{% extends 'base.html.twig' %}

{% block title %}Abonnement{% endblock %}

{% block body %}
    <h1>Abonnement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subscription.id }}</td>
            </tr>
            <tr>
                <th>Start_date</th>
                <td>{{ subscription.startDate ? subscription.startDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>End_date</th>
                <td>{{ subscription.endDate ? subscription.endDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_subscription_index') }}">Retour à la liste</a>

    <a href="{{ path('app_subscription_edit', {'id': subscription.id}) }}">Modifier</a>

    {{ include('subscription/_delete_form.html.twig') }}
{% endblock %}
