<form method="post" action="{{ path('app_tax_delete', {'id': tax.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimmer ce service ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tax.id) }}">
    <button class="btn">Supprimer</button>
</form>
