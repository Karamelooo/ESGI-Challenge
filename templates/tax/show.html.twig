{% extends 'base.html.twig' %}

{% block title %}Taxe{% endblock %}

{% block body %}
    <h1>Taxe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ tax.name }}</td>
            </tr>
            <tr>
                <th>Pourcentage</th>
                <td>{{ tax.percent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tax_index') }}">Retour à la liste</a>

    <a href="{{ path('app_tax_edit', {'id': tax.id}) }}">Modifier</a>

    {{ include('tax/_delete_form.html.twig') }}
{% endblock %}
